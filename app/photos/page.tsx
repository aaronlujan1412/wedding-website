import { UnderConstruction } from "@/components/UnderConstruction";

export default function PhotosPage() {
  return (
    <UnderConstruction
      eyebrow="Photos"
      title="Nothing Here Yet"
      message="Mostly because the wedding hasn't happened. The shared album will show up here afterward, and you'll be able to add your own."
    />
  );
}
